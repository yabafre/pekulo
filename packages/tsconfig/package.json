{
  "name": "@pekulo/tsconfig",
  "version": "0.0.0",
  "private": true,
  "description": "Shared TypeScript presets for Pekulo workspaces (base, apps, packages, next). Also pins the canonical TypeScript version every workspace inherits — single source of truth, no per-workspace drift.",
  "files": [
    "base.json",
    "apps.json",
    "packages.json",
    "next.json"
  ],
  "dependencies": {
    "typescript": "^6.0.3"
  }
}

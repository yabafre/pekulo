nodeLinker: node-modules
yarnPath: .yarn/releases/yarn-4.5.0.cjs
enableGlobalCache: false
# Vercel install runs without committed yarn.lock (bun.lock is SoT) — let
# yarn 4 resolve fresh each CI build instead of refusing to create a lockfile.
enableImmutableInstalls: false

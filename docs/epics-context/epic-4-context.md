# Epic 4 — Context Cache

_Generated by aped-story at 2026-05-20T18:00:00Z. Refreshed 2026-05-20T23:00:00Z after main absorbed PR #86 (R1-R11 audit pass). Consumers: aped-dev, aped-review. Do not hand-edit — re-run aped-story to refresh._

## Post-audit refresh notes (2026-05-20 — PR #86 absorbed)

Main shipped the R1-R11 codification + folder-by-domain restructure (commit 08c9431). Subsequent stories in this epic (4-2, 4-3) MUST follow the new conventions documented in `architecture.md § Audit-derived conventions (2026-05-20 — PR #86)`. Highlights that bind every Epic 4 story:

- **R1 — zod via `@pekulo/zod` only.** Never `import { z } from "zod"` — always `from "@pekulo/zod"`. 4-1 was retroactively aligned in commit a0cc73c.
- **R11 — folder-by-domain in packages.** Schemas live at `packages/validators/src/realestate/realestate.schemas.ts` (NOT a flat `packages/validators/src/realestate.ts`). Same shape for `@pekulo/contracts` (`realestate.contract.ts`) and `@pekulo/types` (`realestate.types.ts`).
- **R8 — visibility default unexported.** Closed-enum literals like `PROPERTY_TYPES_MIRROR` inside validators are file-private `const` ; the SSOT `PROPERTY_TYPES` lives in `@pekulo/types` and is re-exported by the realestate domain barrel.
- **R6 — tag registry forward-pointer policy.** The realestate registry entry in `apps/web/src/lib/zapaction/keys.ts` was kept (NOT pruned by D3) because the realestate oRPC module is mounted (4-1 T16) ; future stories 4-2/4-3/7-1 add cross-feature edges as they ship consumers.
- **R10 — aggregate-root Prisma exception.** Does NOT apply to Epic 4 — each story can own its own `realestate.prisma` since real-estate is a self-contained aggregate cluster.

## Scope from PRD

- FRs in this epic: FR-21 — create property (label, type {residence-principale, locatif, autre}, valuation EUR, last-valued date); FR-22 — attach a single mortgage per property (outstanding principal, annual rate, monthly payment, term months remaining, start date); FR-23 — attach a single rental block per property (monthly rent, monthly charges, furnished flag); FR-24 — derive monthly rental cash-flow (rent − charges − mortgage payment) [4-2]; FR-25 — derive net property equity (valuation − outstanding principal) [4-2]; FR-26 — include net equity in total wealth + compass progress [4-2 / 7-1]; FR-27 — update valuation with audit trail (prior amount preserved).
- NFRs that bind: NFR-1 (compass progress p95 ≤ 300 ms — dashboard must read aggregated realestate equity cheaply); NFR-8 (100 % RLS coverage on new tables); NFR-9 (401 < 100 ms on missing JWT); NFR-15/16 (≤ 100 concurrent users at (b); ≤ 50 properties per user); NFR-22 (WCAG 2.2 AA on Immobilier screen — owned by 4-3); DR-4 (per-row RLS on every user-scoped table); DR-5 (cascade deletion < 60 s — `ON DELETE CASCADE` on property children).

## Architecture references

- Patterns to respect:
  - Module shape (architecture.md § Phase 3 Code Structure) — `apps/api/src/modules/realestate/{realestate.module.ts, realestate.routes.ts, realestate.service.ts, realestate.repository.ts, realestate.errors.ts}` + co-located `*.test.ts` siblings. **Zero `*.types.ts`** (L1 invariant — every type in `@pekulo/types`).
  - **Folder-by-domain in packages (R11 — PR #86).** Realestate slice across the workspace:
    - `packages/validators/src/realestate/realestate.schemas.ts` + `packages/validators/src/realestate/index.ts`
    - `packages/contracts/src/realestate/realestate.contract.ts` + `packages/contracts/src/realestate/index.ts`
    - `packages/types/src/realestate/realestate.types.ts` + `packages/types/src/realestate/index.ts`
    - Cross-domain imports use the domain barrel (`from "../accounts"`), never the implementation file across packages.
  - Layer separation — handler binds to `realestateContract` from `@pekulo/contracts`; service holds business logic + throws typed errors; repository ALWAYS carries `where: { userId: ctx.userId }` (lint-enforced via `pekulo/no-prisma-query-without-user-id`).
  - **Audit history pattern (ADR-0001 — sister tables).** `real_estate_valuations` is an append-only sibling of `real_estate`. RLS = INSERT + SELECT only (no UPDATE / no DELETE). Identical shape to story 1-1 `compass_history` and story 2-2 `account_balance_log`. **Audit count for `rls-audit`: 2 policies.**
  - **Atomic valuation write (architecture L594):** `recordValuation` writes BOTH `RealEstate.currentValuation/lastValuedOn` AND a `RealEstateValuation` row inside the same `prisma.$transaction` — mirrors the 1-1 `Hypothesis + CompassHistory` and 2-2 `Account + AccountBalanceLog` patterns.
  - Pure derive helpers in `apps/api/src/common/derive/`: `rental-cashflow.ts` (FR-24, owned by 4-2), `property-equity.ts` (FR-25, owned by 4-2). No I/O in `common/`. **4-1 ships the domain shape only — derive helpers belong to 4-2.**
  - Prefixed IDs (ADR-0012) registered in `apps/api/src/database/id-prefixes.config.ts`: `RealEstate → "res"`, `RealEstateRental → "resr"`, `RealEstateValuation → "resv"`. **`RealEstateMortgage` prefix is NOT registered yet** — see "Decisions deferred to aped-story" below.
  - Decimal → number boundary — `decimalToNumber(value, fallback)` from `apps/api/src/common/derive/decimal-to-number.ts` (extracted in story 1-1). Every `realestate.repository.ts` read that surfaces `currentValuation`, `outstandingPrincipal`, `monthlyPayment`, `monthlyRent`, `monthlyCharges`, `annualRate` MUST run through `decimalToNumber` — never `Number(decimal)` (L24).
  - oRPC contract mount path — `/rpc/v1/realestate` (already declared in `packages/contracts/src/realestate.contract.ts` as empty `{}` — this epic fills it).
  - Defense in depth (ADR-0013) — `apps/api` connects via Supabase service role (bypasses RLS); explicit `where: { userId }` is the primary guard; Postgres RLS is the safety net. Every Prisma query in the realestate repository carries the userId clause (including the `$transaction` callback's `tx.realEstate.*` / `tx.realEstateValuation.*` calls — the `prismaIdentifier: ["prisma","tx","client"]` override is already in `.oxlintrc.json`).
  - Cross-aggregate guard for mortgage / rental attach: `attachMortgage(propertyId, …)` and `attachRental(propertyId, …)` MUST pre-flight check `findFirst({ where: { id: propertyId, userId } })` before insert — mirrors story 3-1's `findAccountForUser` cross-aggregate probe.
- ADRs in scope (5 of these were amended by PR #86 — read the amendments before authoring 4-2/4-3):
  - `docs/adr/0001-audit-history-via-sister-tables.md` — sister-table audit pattern. **Authoritative for `real_estate_valuations`**.
  - `docs/adr/0007-design-system-tamagui-migration-now.md` — **amended by PR #86** (R7 — `@pekulo/ui` barrel discipline ; provider must not import from components/).
  - `docs/adr/0009-elysia-orpc-with-zapaction-bridge.md` — **amended by PR #86** (R3/R4 — ZapAction is the only allowed React-Query consumer in `apps/web` hooks ; tag registry centralises invalidation).
  - `docs/adr/0010-hooks-orchestration-boundary.md` — **amended by PR #86** (R9 — optimistic mutation recipe with ZapAction + `useQueryClient` only inside `onMutate/onError/onSettled`). Relevant when 4-3 UI consumes the 4-1 contract.
  - `docs/adr/0011-packages-reorg-pekulo-namespace.md` — **amended by PR #86** (R1 — `@pekulo/zod` is the sole zod entry point ; R2 — types→validators layering re-affirmed ; R11 — folder-by-domain layout).
  - `docs/adr/0012-prisma-7-schema-folder-prefixed-ids.md` — **amended by PR #86** (R10 — aggregate-root Prisma exception : `accounts.prisma` legitimately co-houses holdings ; new aggregates with a shared root co-locate in a single file).
  - `docs/adr/0013-prisma-rls-defense-in-depth.md` — Prisma service-role bypass + explicit `userId` guard.
  - `docs/adr/0014-prisma-migrations.md` — Prisma migrate is the forward-only migration tool; **manual SQL pattern from stories 1-1 / 2-1 / 2-2 / 3-1 applies** (Supabase pooler hang precedent — use `prisma:migrate:deploy` with `DATABASE_URL=$DIRECT_URL`).

## UX references

- Screens: **Immobilier** (`/dashboard/immobilier`, screen 6) — Hero (Equity nette · valuation · debt remaining), pill `Ajouter un bien`, `PropertyCard` per property (valuation, mortgage breakdown, % remboursé donut). **4-1 is the API/domain port — UI surfaces are owned by story 4-3.** No UI work in 4-1.
- Components consumed by 4-3 (forward-pointer, not in 4-1 scope): `PropertyCard`, `property-create-form.tsx`, `mortgage-form.tsx`, `rental-form.tsx`, `valuation-update-form.tsx`.

## Project context (brownfield only)

- **Greenfield module.** No brownfield `real_estate*` tables exist in `apps/web/supabase-schema.sql` — this is a net-new domain (PRD Phase 1 / scope row 5). Net-new tables: `real_estate`, `real_estate_rental`, `real_estate_valuations` (+ `real_estate_mortgage` if the discussion point lands on "separate entity"). All 4 tables get prefixed-ID columns from day 1 (no UUID → TEXT migration dance like 2-1 / 3-1 required).
- Schema file: create new `apps/api/prisma/schema/realestate.prisma` (no `accounts.prisma`-style grouping — 4-1 owns its own file, mirrors `compass.prisma` and `milestones.prisma` precedent).
- Migration strategy: manual SQL under `apps/api/prisma/migrations/<timestamp>_create_realestate/migration.sql` — same Supabase pooler deviation precedent as stories 1-1 T1, 2-1 T1, 2-2 T1, 3-1 T1. Apply via `bun --filter='@pekulo/api' run prisma:migrate:deploy`. The migration must:
  1. CREATE TABLE the 3-or-4 tables with `id TEXT PK`, `user_id UUID NOT NULL`, FK to `auth.users(id) ON DELETE CASCADE`.
  2. CREATE INDEX on `(user_id, created_at desc)` for `real_estate`; `(user_id, real_estate_id, valued_on desc)` for the valuation log; `(user_id, real_estate_id)` for rental + mortgage 1:1 children.
  3. ALTER TABLE … ENABLE ROW LEVEL SECURITY on every table.
  4. CREATE POLICY — **4 policies (SELECT/INSERT/UPDATE/DELETE)** on `real_estate`, `real_estate_rental`, `real_estate_mortgage` (CRUD tables); **2 policies (SELECT + INSERT only)** on `real_estate_valuations` (audit sister, ADR-0001).
  5. `auth.uid() = user_id` USING / WITH CHECK clauses on every policy.
- RLS audit expected counts (must be added to `apps/api/scripts/rls-audit.ts#EXPECTED_POLICY_COUNTS`):
  - `real_estate: 4`
  - `real_estate_rental: 4`
  - `real_estate_mortgage: 4` (only if the "separate entity" decision lands)
  - `real_estate_valuations: 2`
- Cache invalidation graph (forward-pointer to 4-3 / 7-1) — wire `realestate` invalidation tag into `apps/web/src/lib/zapaction/keys.ts` `setTagRegistry({...})`. Mirror story 3-1 T17 forward-pointer.
- Brownfield modules to mine for read-pattern parity: `apps/web/src/lib/{actions,data,schemas}/realestate.*` — **do not exist** (greenfield); no brownfield port required.
- Existing oRPC scaffold confirmed empty at `packages/contracts/src/realestate/realestate.contract.ts` (R11 path post-PR #86 ; line `realestateContractV1 = {} as const`) — 4-1 fills this with the 12-procedure surface.
- **Decisions locked at aped-story step 02 (user-confirmed 2026-05-20):**
  - **D1 — RealEstateMortgage = separate Prisma model.** Prefix `resm` (to be added to `id-prefixes.config.ts`), FK to `RealEstate.id` with `ON DELETE CASCADE`, unique constraint on `realEstateId` enforces 1:1. RLS audit must add `real_estate_mortgage: 4`. `architecture.md L815` is stale — flag in next doc sweep.
  - **D2 — Mortgage / rental verbs = separate lifecycle.** `attachMortgage` (create) + `updateMortgage` (edit) + `detachMortgage` (delete) ; same for rental. Matches `compass.updateCompass` / `holdings.recordLot` / `accounts.recordBalanceChange` precedent.
  - **D3 — `RealEstate.currentValuation` mirrors latest valuation row.** Adopt the precedent from 1-1 (`Hypothesis.objectif` + `CompassHistory.objectif`) and 2-2 (`Account.cashBalance` + `AccountBalanceLog.cashBalance`). Latest on `RealEstate`, full history in `RealEstateValuation`. `recordValuation` writes both inside `prisma.$transaction`.
  - **D4 — `attachMortgage` / `attachRental` = separate calls (NOT atomic with `createProperty`).** UI in 4-3 owns the multi-step form orchestration. Cross-aggregate `findRealEstateForUser(userId, propertyId)` guard before each attach (mirrors story 3-1's `findAccountForUser`).

## Lessons applicable

- **2026-05-20 — `@pekulo/zod` is the SOLE zod entry point (R1, PR #86 — amends ADR-0011)** (Scope: aped-story, aped-dev, aped-review).
  - Rule: NEVER `import { z } from "zod"` or `import { ZodError } from "zod"`. Always `from "@pekulo/zod"`. Applies to every file under `packages/*` and `apps/*` (including `*.test.ts`).
  - Why: enforces one zod version across the monorepo and gives a single migration hook for future zod major bumps. Future Pekulo helpers (Money, EuroAmount, IsoDate, Percent, tabularNum) will also ship from `@pekulo/zod`.
  - How to apply: stories 4-2 / 4-3 use `import { z } from "@pekulo/zod"` everywhere ; `@pekulo/zod` is already declared as `workspace:*` in `apps/api/package.json` and `packages/validators/package.json`. Custom oxlint rule `pekulo/no-direct-zod-import` is planned to enforce.
- **L1 — Zero `*.types.ts` inside `apps/api/src/modules/**`\*\* (Scope: aped-arch, aped-dev, aped-review).
  - Rule: every TS type lives in `@pekulo/types`. Internal adapter interfaces co-located inside `*.module.ts` / `*.service.ts` / `*.repository.ts` are allowed (precedent: story 3-2 `holdings.cache.ts` co-locates `CacheEntry`).
  - Why: prevents drift between API-internal types and the public domain model.
  - How to apply: `RealEstate`, `RealEstateMortgage`, `RealEstateRental`, `RealEstateValuation`, `RealEstateId`, `RealEstateMortgageId`, etc. all live in `@pekulo/types/src/index.ts` as `z.infer` re-exports from `@pekulo/validators/src/realestate.ts` + branded IDs. **Never create `apps/api/src/modules/realestate/realestate.types.ts`.**
- **L8 — Elysia 1.4 `Elysia` type is invariant** (Scope: aped-arch, aped-dev — story 4-1 explicitly listed in the lessons.md scope list at L233).
  - Rule: never annotate variables/parameters as bare `Elysia`. Rely on inference at the module factory boundary or use `AnyElysia` for cross-cutting utilities.
  - Why: explicit `Elysia` annotations break compilation when the chain accumulates routes / lifecycle hooks.
  - How to apply: `createRealestateModule(deps)` returns `RealestateModule = { service, router: ReturnType<typeof createRealestateRouter> }`. AC must include a grep guard: `grep -rEn ':\s*Elysia\b|as\s+Elysia\b|<Elysia\b' apps/api/src/modules/realestate | wc -l` returns `0`.
- **L24 — `Number(decimal)` silently truncates above MAX_SAFE_INTEGER** (Scope: aped-arch, aped-dev — story 4-1 explicitly listed in the lessons.md scope list at L233).
  - Rule: coerce via `.toNumber()` (or the shared `decimalToNumber(value, fallback)` helper at `apps/api/src/common/derive/decimal-to-number.ts`).
  - Why: silent precision loss on monetary fields at large valuations (a Persona-#1 property valuation will stay well under MAX_SAFE_INTEGER, but the rule is invariant).
  - How to apply: every `realestate.repository.ts` boundary that surfaces `currentValuation`, `outstandingPrincipal`, `monthlyPayment`, `annualRate`, `monthlyRent`, `monthlyCharges` runs through `decimalToNumber(row.X, 0)`. AC asserts `grep -rn 'Number(.*Decimal' apps/api/src/modules/realestate | wc -l` returns `0`.
- **2026-05-19 — `bun --filter='@pekulo/api'` (NOT `bun --filter=api`)** (Scope: aped-story, aped-dev, aped-review).
  - Rule: workspace `--filter` MUST use the full `package.json#name`. Pekulo workspaces are namespaced `@pekulo/*`. **`bun --filter=api …` exits `No packages matched the filter`.**
  - How to apply: every command in the story file uses `bun --filter='@pekulo/api' run …` (quoted to prevent shell glob expansion). Fan-out across workspaces: `bun --filter='@pekulo/*' run <script>`.
- **2026-05-07 — `bun test` ≠ `vitest run`** (Scope: aped-dev, aped-review).
  - Rule: `apps/api`'s `test` script runs `bun test` (Bun's native runner). All new `*.test.ts` under `apps/api/src/**` use `import { describe, expect, mock, test } from "bun:test"` — never vitest globals.
- **2026-05-05 — `bun --cwd <relative> run <script>` silently fails** (Scope: aped-dev, aped-story).
  - Rule: use `bun --filter='@pekulo/api' run <script>` or `cd apps/api && bun run <script>`. Never `bun --cwd apps/api run …` in story commands.
- **2026-05-04 — Manual SQL migrations preferred over `prisma migrate dev` on this repo** (Scope: aped-arch, aped-dev — codified by stories 1-1, 2-1, 2-2, 3-1 deviations).
  - Rule: write the migration SQL by hand under `apps/api/prisma/migrations/<timestamp>_<name>/migration.sql`, then apply via `bun --filter='@pekulo/api' run prisma:migrate:deploy` (which sets `DATABASE_URL=$DIRECT_URL` per `prisma.config.ts`).
  - Why: `prisma migrate dev` hangs against the Supabase pooler. The deploy path uses the direct connection and works reliably.
- **L25 — Story-spec UX placement MUST be cross-checked against `docs/ux-preview/src/App.tsx`** (Scope: aped-story, aped-arch, aped-dev).
  - How to apply: 4-1 is API-only — the Pre-Implementation Checklist must confirm no UI claim is embedded. UI surfaces belong to 4-3.
- **2026-05-20 — `defineAction` discriminated-union envelope returns MUST omit `output:`** (Scope: forward-pointer to 4-3 / future write-path SAs).
  - Not directly applicable to 4-1 (no web-tier SA in this story), but the contract surface defined here must allow envelope-shaped returns from any future SA wrapper that propagates `REALESTATE_NOT_FOUND` / `MORTGAGE_ALREADY_ATTACHED` / `RENTAL_ALREADY_ATTACHED` codes. The oRPC `errors` declaration is the right place — see story 3-1 `holdings.contract.ts` for the precedent.
- **2026-05-04 — Elysia `NotFoundError` masquerades as vanilla Error** (Scope: aped-dev — apps/api error-mapper).
  - Already handled in `apps/api/src/platform/http/error-mapper.ts` since story 0-5; 4-1 inherits this — no new mapper branch needed unless a new error class lands.

## Previous stories — outcomes

### Story 4-1-realestate-domain — done 2026-05-21T00:42:00Z

- **Decisions:**
  - Module shape mirrors holdings 3-1 (factory `createRealestateModule({prismaService}) → {service, router}`, oRPC contract-first at `/rpc/v1/realestate`). 4-2 / 4-3 build on this — do not re-design.
  - `recordValuation` uses `tx.realEstate.updateMany` inside `$transaction` (not `update`) — eliminates Prisma 5+ relaxed `WhereUniqueInput` dependency and naturally races concurrent deletes to a 404 instead of leaking P2025. **Apply same pattern in 4-2 derive writes if they touch `RealEstate`.**
  - `RealestateError` factory pattern (constructor narrows `code` from `PekuloErrorCode` to `RealestateErrorCode`) is the canonical shape — 4-2/4-3 errors inherit it.
  - Cross-aggregate guard `requireOwnedProperty(userId, propertyId)` in service runs BEFORE every mutation ; the repo's `findByIdForUser` is the load-bearing primitive (mirrors 3-1 `findAccountForUser`).
- **Files:** see story 4-1 File List (14 NEW + 13 MODIFIED, post-review-supp).
- **Contracts:**
  - 12-procedure oRPC `realestateContract` mounted at `/rpc/v1/realestate` (T13).
  - 5 new `PekuloErrorCode`s: `REALESTATE_NOT_FOUND` (404), `MORTGAGE_NOT_FOUND` (404), `RENTAL_NOT_FOUND` (404), `MORTGAGE_ALREADY_ATTACHED` (409), `RENTAL_ALREADY_ATTACHED` (409).
  - 4 new Prisma models: `RealEstate` (prefix `res`), `RealEstateMortgage` (`resm`, UNIQUE on `real_estate_id`), `RealEstateRental` (`resr`, UNIQUE on `real_estate_id`), `RealEstateValuation` (`resv`, append-only sister with INSERT+SELECT-only RLS).
  - `RecordValuationOutcome` / `MortgageUpdateOutcome` / `RentalUpdateOutcome` discriminated unions surface "not-found" cleanly — 4-2 derive layer reads `RealEstate` so the type signature stays stable.
  - `apps/web/src/lib/zapaction/keys.ts` declares `REALESTATE_KEY` + `realestateKeys` (`list` / `byId` / `valuations`) + `realestateTags`. **4-2 / 4-3 consumers reuse this set ; don't re-declare.**
- **Deviations from plan:**
  - `listPropertiesOutputSchema` / `listValuationsOutputSchema` / `realestateOkSchema` live in `@pekulo/validators` (not inlined in contract) — keeps `@pekulo/contracts` zod-free.
  - Integration UUIDs use RFC-4122 v4 fixtures (`aaaaaaaa-aaaa-4aaa-…`), not the relaxed `00000000-…-a` shape. Schema's `z.string().uuid()` enforces this strictly.
  - `recordValuation` discriminated outcome shape (`{ok | not-found}`) added during aped-review-supp ; service contract unchanged externally (still throws `realestateNotFound()`).
  - Integration test binds `port: 0` (Bun OS-assigned), not `PORT_BASE + Math.random()`. Other sibling integration tests still use the legacy pattern.
  - Story Dev Notes claim `.partial()` on `updateMortgage/Rental` schemas ; actual implementation is `.optional()` + `.refine()` rejecting empty payloads (stricter — caller must supply ≥1 field).

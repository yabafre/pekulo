fastapi==0.115.6
uvicorn[standard]==0.33.0
yfinance==0.2.51
pydantic==2.10.4
# curl_cffi gives yfinance a real-browser TLS fingerprint (auto-used if installed).
# Bypasses many Yahoo IP/UA blocks, especially from EU residential networks.
curl_cffi==0.7.4

# Epic 2 — Context Cache

_Generated by aped-story at 2026-05-15T15:00:00Z. Consumers: aped-dev, aped-review. Do not hand-edit — re-run aped-story to refresh._

## Scope from PRD

- FRs in this epic:
  - FR-9 — Create account (label, type ∈ {livret, pea, cto, av, autre}, base currency, optional cash balance). Story 2-1 (API), 2-3 (UI).
  - FR-10 — Edit / delete account, deletion blocked when at least one holding OR transaction still references it. Story 2-1 (API), 2-3 (UI).
  - FR-11 — Record a manual cash-balance change on an account at a given date. Story 2-2 (API), 2-3 (UI).
  - FR-12 — List authenticated user's accounts (per-row RLS enforced). Story 2-1 (API), 2-3 (UI surfaces via Patrimoine tab).

- NFRs that bind:
  - NFR-1 — Compass-progress p95 < 300 ms on `/dashboard` (transitively — cash balances feed total wealth via FR-43).
  - NFR-8 — 100 % per-row RLS coverage via `auth.uid() = user_id` on every user-scoped table; brownfield `accounts` table already covered, `account_balance_log` (new in 2-2) must inherit the quartet.
  - NFR-9 — Reject unauth requests within 100 ms. `requireUserContext()` helper on every accounts route.
  - NFR-12 — Zero PII in LLM prompts (not directly load-bearing here, but accounts surface MUST NOT seep into prompt builder).
  - NFR-16 — Single-user volume up to 50 000 tx / 500 holdings / 50 properties. Accounts list itself is small (≤ 50 rows) so no cursor pagination needed; the delete FK guard query must remain index-friendly.
  - NFR-28 — Web tier opens 0 direct DB connections; `apps/api` is the sole Postgres consumer. The brownfield `apps/web/src/lib/actions/portfolio.ts` Supabase-direct reads/writes for accounts MUST be removed from the web tier as part of 2-1 (or in 2-3 as part of the UI port, per the dev plan).

- DRs that bind:
  - DR-4 — RLS quartet on every user-scoped table; audit tables INSERT+SELECT only.

## Architecture references

- **Patterns to respect:**
  - **Module factory shape (ADR-0009)** — `apps/api/src/modules/accounts/{accounts.module.ts, accounts.routes.ts, accounts.service.ts, accounts.repository.ts, accounts.errors.ts}` plus `*.test.ts` siblings. Factory returns `{ router, service }` (mirrors `createMilestonesModule` / `createCompassModule`). `router` type inferred via `ReturnType<typeof createAccountsRouter>`. **L8 (Elysia 1.4 invariant) explicitly cites story 2-1 — never annotate `Elysia`, infer the chain or use `AnyElysia` at cross-cutting boundaries.**
  - **Hard layering (ADR-0010, lint-enforced via story 0-12)** — Component → Custom Hook → Server Action (`<feature>-actions.ts`) → oRPC client → Elysia handler → service → repository → Prisma. Lint rules `no-server-action-in-component`, `no-cross-feature-action-import`, `no-prisma-query-without-user-id` all gate this path. Story 2-1 is API-only — the lint rules that bite here are `no-prisma-query-without-user-id` on `accounts.repository.ts` and the `prismaIdentifier: ["prisma","tx"]` override that already covers `$transaction(tx => …)` usage.
  - **RLS defense in depth (ADR-0013)** — every Prisma query carries explicit `where: { userId: ctx.userId, … }`; RLS stays active even if `apps/api` omits the guard. Defense in depth = explicit guard on EVERY query touching `Account`, including the FK-guard pre-delete probes against `Holding` and `Transaction`.
  - **Prisma error mapping** — repository wraps Prisma errors via `prisma-error-mapper.ts` → `RlsViolationError` on `P2025` if the row didn't match the userId guard. Surface `ReferencedFKError` (new code, see Architecture L572's open list) when the FK guard fires manually OR Prisma's `P2003` foreign-key-constraint code bubbles up.
  - **Decimal coercion (L24, 2026-05-04 — explicitly names story 2-1)** — `Account.cashBalance` is Prisma `Decimal` (column `cash_balance NUMERIC`). The repository → service → DTO boundary MUST use `decimalToNumber()` from `apps/api/src/common/derive/decimal-to-number.ts` (extracted in story 1-1). Inlining `Number(decimal)` or duplicating the helper is a review fail.
  - **Prefixed IDs vs brownfield UUID (ADR-0012 + story 1-4 precedent)** — the brownfield `accounts` table uses `id UUID PRIMARY KEY DEFAULT gen_random_uuid()` (see `apps/web/supabase-schema.sql:185`). `id-prefixes.config.ts` currently registers `Account: "acc"` — this is a precedent collision with the UUID column type (PostgreSQL rejected `id = "hyp_<base62>"` against `id uuid` during story 1-4). Story 2-1 MUST either (a) flip `Account` to `null` in `ID_PREFIXES` (mirror Hypothesis), letting Postgres' `gen_random_uuid()` default fire — preferred per brownfield-exception note; OR (b) introduce a migration that converts the UUID column to TEXT and applies prefixed IDs. **Choose (a) at story design time** unless the story explicitly opts for the migration; document the choice in the story. Same logic propagates to story 3-1 (Holding, HoldingLot) and story 5-1 (Transaction) — all brownfield UUID-keyed tables.
  - **Migration discipline (ADR-0014)** — `prisma migrate dev --name <verb_noun>` writes `apps/api/prisma/migrations/<timestamp>_<verb_noun>/`. Forward-only. **No new migration is strictly required for 2-1** — the `accounts` table, RLS policies, and AccountType enum already exist in the brownfield SQL (`apps/web/supabase-schema.sql`) and were captured in `apps/api/prisma/schema/accounts.prisma` during story 0-4. If the story chooses option (b) above, a migration lands here; otherwise no schema change ships. The `rls-audit` CI probe (`apps/api/scripts/rls-audit.ts`) already asserts the `accounts` quartet — no new entry needed.
  - **Naming (Phase 3)** — Prisma model `Account` PascalCase (already declared); table `accounts` (brownfield, `@@map("accounts")` already in place); contract module key `accounts`, mount path `/rpc/v1/accounts`.
  - **Pagination convention (D2)** — accounts list is bounded (≤ 50 rows / user in any realistic personal-finance scenario); cursor pagination is NOT required. Return the full list ordered by `createdAt asc` (matches brownfield `apps/web/src/lib/actions/portfolio.ts#getAccounts`).
  - **Domain types live in `@pekulo/types` (L1, 2026-05-09 — explicitly cites every apps/api module)** — `Account`, `AccountInput`, `AccountType` (already an `as const` union literal), `ReferencedFKError` payload typed in `@pekulo/types` re-export of Zod-inferred validators. **Zero `accounts.types.ts` file inside `apps/api/src/modules/accounts/`** — internal `AccountsDeps` interface lives next to the factory in `accounts.module.ts` or `accounts.service.ts`, never in a `*.types.ts` file (story 1-2 enforced this invariant for compass & milestones).

- **ADRs in scope:**
  - `docs/adr/0009-elysia-orpc-with-zapaction-bridge.md` — module factory + oRPC mount, contract-first.
  - `docs/adr/0010-hooks-orchestration-boundary.md` — hard layering (forward-pointer for 2-3 UI).
  - `docs/adr/0012-prisma-7-schema-folder-prefixed-ids.md` — Account prefix registration + brownfield UUID exception.
  - `docs/adr/0013-prisma-rls-defense-in-depth.md` — explicit `where: { userId }` clause + lint rule.
  - `docs/adr/0014-prisma-migrations.md` — Prisma migrate + manual RLS policy append (not required for 2-1 unless option (b)).

## UX references

- **Screens (from `docs/ux/screen-inventory.md`):**
  - Screen 2 — Cap · Patrimoine view (`/dashboard?tab=patrimoine`) — `AccountsSection` + total-liquide hairline. Story 2-3 ships the UI; stories 2-1 and 2-2 ship API-only.
  - Screen 7 — Paramètres (`/dashboard/parametres`) — `accounts-section.tsx` (forms surface, story 2-3).
- **Components (from `docs/ux/components.md`):**
  - `AccountsSection` + `AccountRow` — FR-9, FR-10, FR-12 (UI only, story 2-3).
  - `account-create-form.tsx`, `account-edit-form.tsx`, `account-delete-confirm.tsx` — FR-9, FR-10 (UI only, story 2-3). The delete-confirm dialog MUST surface `ReferencedFKError` from the API in the confirmation flow (per epic 2 AC-2 on story 2-3).
  - `account-balance-form.tsx` — FR-11 (UI only, story 2-3, depends on 2-2 API).

> Stories 2-1 and 2-2 are **API-only**. UI surfaces ship in 2-3 (which depends on 2-1 + 2-2 + 0-10-pekulo-ui-migration).

## Project context (brownfield only)

- **Brownfield accounts CRUD lives at `apps/web/src/lib/actions/portfolio.ts`** (lines 52–123: `getAccounts`, `saveAccount`, `deleteAccount` — `defineAction` over Supabase JS, `eq("user_id", ctx.userId)` explicit). Story 2-1 ports this to `apps/api/src/modules/accounts/` with the FK guard + explicit `where: { userId }` clauses. The web-tier zapaction wrappers stay during the port (per ADR-0009) — they become thin `'use server'` shims over the oRPC client when story 2-3 lands.
- **Brownfield SQL schema (`apps/web/supabase-schema.sql:184–227`)**:
  - `accounts` table: `id UUID PRIMARY KEY DEFAULT gen_random_uuid()`, `user_id UUID NOT NULL REFERENCES auth.users(id) ON DELETE CASCADE`, `label TEXT NOT NULL`, `type account_type NOT NULL`, `currency TEXT NOT NULL DEFAULT 'EUR'`, `cash_balance NUMERIC NOT NULL DEFAULT 0 CHECK (cash_balance >= 0)`, `notes TEXT`, `created_at`, `updated_at`.
  - `account_type` enum: `('livret', 'pea', 'cto', 'av', 'autre')`.
  - RLS quartet already in place (SELECT/INSERT/UPDATE/DELETE policies, all `auth.uid() = user_id`). No new RLS work required unless 2-1 migrates the column type.
  - FK references: `holdings.account_id REFERENCES accounts(id) ON DELETE CASCADE`. The cascade means a brute `accounts.delete` would silently nuke holdings — **the FK guard MUST be enforced at the service layer BEFORE the Prisma delete fires** (AC-1 of story 2-1).
- **Brownfield Prisma model (`apps/api/prisma/schema/accounts.prisma:4–18`)** — `Account` model already declared with `@@map("accounts")`, `cashBalance` as `Decimal @default(0) @db.Decimal`, `holdings Holding[]` relation. No schema change needed.
- **Existing patterns to mirror (from epic 1):**
  - `decimalToNumber()` helper at `apps/api/src/common/derive/decimal-to-number.ts` (story 1-1 extract) — apply at the row → DTO boundary for `cashBalance`.
  - `createXxxModule(deps): { router, service }` factory pattern (`milestones.module.ts:22–30`, `compass.module.ts`, `hypothesis.module.ts`).
  - `implement(contract).$context<{ userId, email }>().router({ … })` oRPC handler shape — context injection via `mountOrpc`, handlers throw `PekuloError("UNAUTHORIZED", …)` when context missing.
  - `addEnforcingCap`-style `$transaction`-wrapped check-then-act (`milestones.repository.ts`) — applicable here for the FK pre-delete probe + delete in one transaction to avoid TOCTOU.
  - `prismaService.client.account.findMany({ where: { userId }, orderBy: { createdAt: "asc" } })` mirrors brownfield ordering.
- **Web tier still uses zapaction (story 0-6 bridge)**: 2-3 will rewire `getAccounts`/`saveAccount`/`deleteAccount` from direct Supabase calls (`apps/web/src/lib/actions/portfolio.ts:52–123`) to thin `'use server'` wrappers over the oRPC accounts client. Story 2-1 does NOT touch the web tier — its scope ends at the oRPC contract + Elysia mount.
- **Contract scaffold is empty** — `packages/contracts/src/accounts.contract.ts` currently exports `accountsContractV1 = {} as const`. Story 2-1 lands the procedure definitions: `create`, `update`, `delete`, `list`.
- **No validators yet** — `packages/validators/src/` has `compass.ts`, `hypothesis.ts`, `milestones.ts`. Story 2-1 lands `packages/validators/src/accounts.ts` with `createAccountInputSchema`, `updateAccountInputSchema`, `deleteAccountInputSchema`, `accountSchema`. Re-export from `packages/validators/src/index.ts` AND surface domain types from `@pekulo/types` (NOT a module-local `*.types.ts`).

## Lessons applicable

- **L1 — 2026-05-09 — Zero `*.types.ts` files inside `apps/api/src/modules/**`\*\* (Scope: aped-arch, aped-dev, aped-review — every apps/api module + every package re-exporting domain types)
  - Rule: every TS type lives in `@pekulo/types`, including internal API adapter interfaces. The accounts module MUST export `Account`, `AccountInput`, `ReferencedFKErrorPayload`, etc. via `@pekulo/types` re-export of Zod-inferred shapes from `@pekulo/validators#accounts.ts`. Internal `AccountsDeps` lives in `accounts.module.ts` or `accounts.service.ts`, never in `accounts.types.ts`. Story 1-2's mid-story refactor (commit `87f137d`) established this — apply on story 2-1 from the start.

- **L8 — 2026-05-04 — Elysia 1.4 `Elysia` type is invariant** (Scope: aped-arch, aped-story, aped-dev — explicitly lists **story 2-1**)
  - Rule: never annotate variables/parameters as bare `Elysia`. Module factories let TS infer chains; cross-cutting boundaries import `AnyElysia` from `elysia`. The accounts module's router type should follow the same shape as `milestones.module.ts:18` — `router: ReturnType<typeof createAccountsRouter>`.

- **L24 — 2026-05-04 — `Number(decimal)` silently truncates above MAX_SAFE_INTEGER** (Scope: aped-arch, aped-dev — explicitly lists **story 2-1**)
  - Rule: coerce Prisma `Decimal` via `.toNumber()` at the boundary. Apply to `cashBalance` (column type `Decimal`) when reading from `Account`. Existing `decimalToNumber()` at `apps/api/src/common/derive/decimal-to-number.ts` is the canonical helper — duplicate or inline = review fail.

- **L25 — 2026-05-04 — `AsyncLocalStorage.enterWith` correctness rests on Next.js per-request `ResourceContext` isolation** (Scope: aped-arch, aped-dev — stories 0-6, 0-7, every apps/web feature port)
  - Rule: re-verify on every Next minor bump. Story 2-1 is API-only; rule applies forward to story 2-3's web hook work, not to this story.

- **L23 — 2026-05-04 — Bun `--frozen-lockfile` in Docker requires every workspace member's `package.json`** (Scope: aped-arch, aped-story, aped-dev — every `apps/*` Dockerfile)
  - Rule: when adding new packages, ensure `.dockerignore` re-includes manifests via `!path` exceptions. Story 2-1 does not add a new workspace; rule informs architecture only.

- **Story 0-12 lint rules now active** (general — applies to every API story from epic 1 onward):
  - `pekulo/no-prisma-query-without-user-id` — every `prisma.account.*`, `prisma.holding.*` (FK probe), `prisma.transaction.*` (FK probe) query in `accounts.repository.ts` MUST include `where: { userId }`. The rule's `prismaIdentifier` accepts `string | string[]` — `$transaction(async (tx) => …)` covered via the existing `["prisma","tx"]` override.
  - `pekulo/no-server-action-in-component` — irrelevant to story 2-1 (API-only); bites in 2-3.
  - `pekulo/no-cross-feature-action-import` — `accounts-actions.ts` (when 2-3 lands) MUST NOT import from `portfolio-actions.ts` / `holdings-actions.ts`. Cross-feature work goes via global hooks layer or a separate oRPC contract method.
  - `pekulo/no-tailwind-outside-ui` — irrelevant to story 2-1.

- **L11 (2026-05-13) — Tamagui `styled(View, ...)` rejects text-style props** — irrelevant to story 2-1 (API-only).

- **Brownfield UUID-column precedent (story 1-4 decisions)** — `Hypothesis` registered with `null` in `id-prefixes.config.ts` because the brownfield column is `id UUID DEFAULT gen_random_uuid()`. Apply the same to `Account` (currently registered with `"acc"`) unless the story explicitly opts for a column-type migration. Mirror for `Holding`, `HoldingLot` in story 3-1 and `Transaction` in story 5-1.

## Previous stories — outcomes

### Story 2-1-accounts-orpc-port — done 2026-05-16T14:51:00Z

- **Decisions:**
  - **Override of story 1-4 brownfield-UUID precedent for `Account`** — instead of `Account: null` in `id-prefixes.config.ts`, the column is migrated UUID → TEXT and every existing row is re-id'd in-place to `acc_<base62-21>`. The override stays scoped to `Account`; story 3-1 (Holdings) and 5-1 (Transactions) remain free to choose either path per their own scope review.
  - **FK-probe + delete TOCTOU lives at the repository layer** — `accounts.repository.ts#deleteWithFkProbe` wraps both calls in a single `$transaction`. The service has no transaction-runner dep; the module factory has no `runTx` indirection / no tx-narrowed cast. This is the canonical shape for future FK-guarded deletes (transactions in 5-1, holdings in 3-1 if applicable).
  - **Lint rule `pekulo/no-prisma-query-without-user-id` covers `deps.client.X.Y` from now on** — `.oxlintrc.json` extends `prismaIdentifier` to `["prisma","tx","client"]`. Future repos using `deps.client.account.findMany(...)` (or similar) without an explicit `where: { userId }` will fail lint at commit time.
- **Files:**
  - Created: migration SQL, `packages/validators/src/accounts.ts`, `apps/api/src/modules/accounts/{errors,repository,service,routes,module}.ts` + 4 test siblings.
  - Modified: `packages/contracts/src/accounts.contract.ts`, `packages/validators/src/index.ts`, `packages/types/src/index.ts`, `packages/ui/src/components/PekuloAccountRow.tsx` + `PekuloAccountsSection.tsx`, `apps/api/src/bootstrap/runtime-dependencies.ts`, `apps/api/src/common/errors/pekulo-error.ts`, `apps/api/src/platform/http/error-mapper.ts`, `.oxlintrc.json`.
  - T1 enablement sidecars (out-of-story but landed on the same branch, judged legitimate): `apps/api/prisma.config.ts`, `apps/api/package.json` (+ `dotenv` devDep), root `package.json` (`prisma:*` + `db:rls-audit` scripts wrapped in `dotenv-cli`).
- **Contracts:**
  - `packages/contracts/src/accounts.contract.ts` exports 4 oRPC procedures (`create`, `update`, `delete`, `list`) under `accountsContractV1`; mount path `/rpc/v1/accounts`; `PekuloRpcRouter.accounts` registered.
  - `packages/validators/src/accounts.ts` exports `accountSchema`, `createAccountInputSchema`, `updateAccountInputSchema`, `deleteAccountInputSchema`, `deleteAccountOutputSchema`, `listAccountsOutputSchema`, plus inferred TS types `Account`, `CreateAccountInput`, `UpdateAccountInput`, `DeleteAccountInput`. `Account` is the canonical domain entity (re-exported from `@pekulo/types`); `AccountCardItem` is the UI prop shape (used by `PekuloAccountRow` / `PekuloAccountsSection`).
  - `PekuloErrorCode` gains `ACCOUNT_NOT_FOUND` (404) and `ACCOUNT_REFERENCED_FK` (409); `error-mapper.ts` `Record<PekuloErrorCode, number>` exhaustive type enforces compile-time coverage.
- **Deviations from plan:**
  - **T13 integration test** ships 5 real tests (create / list / list 401 / update / delete happy) — FK 409 + cross-user 404 deliberately _not_ asserted at HTTP boundary because RPCHandler wraps handler-thrown errors before the Elysia error-mapper sees them (same precedent the milestones integration test documents). Service-layer + module-layer tests cover the typed error codes.
  - **T10 / T11 swapped order** at commit time — contract first, routes second, so typecheck stayed at exit 0 on every commit.
  - **T1 deploy required two workaround steps**: (1) export `DATABASE_URL="$DIRECT_URL"` for `prisma migrate deploy` (Supabase transaction pooler hangs); (2) `prisma migrate resolve --applied <name>` for the 4 brownfield migrations that had been applied out-of-band before this story. Codified in `apps/api/prisma.config.ts`.
  - **Review-cycle hardening (2026-05-16)** added 3 fix commits on top of the dev branch: lint blind-spot widen, migration idempotency + cursor snapshot, repository `deleteWithFkProbe` refactor (TOCTOU moved to repo, no tx-narrowed cast). Repository test count rose to 25 / 0 fail / 67 expects.

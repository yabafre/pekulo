# Epic 11 — Context Cache

_Generated by aped-story at 2026-05-28T19:01:18Z. Consumers: aped-dev, aped-review. Do not hand-edit — re-run aped-story to refresh._

## Scope from PRD

- FRs in this epic: FR-49 (data export — 11-1), FR-50 (account deletion — 11-2). 11-3 / 11-4 / 11-6 carry no own FR (they gate DRs + NFRs). 11-5 — EU AI Act transparency notice.
- NFRs that bind: NFR-8 (100 % RLS coverage with a build-failing automated check), NFR-14 (at-rest encryption documented), NFR-6 / NFR-7 (60 s export / deletion budgets), NFR-30 (100 MB export ceiling), NFR-21 (restore drill), NFR-22 / NFR-23 / NFR-24 (WCAG / axe), NFR-31 (Bridge token storage at rest), NFR-33 (webhook signature). DRs: DR-4 (per-row RLS on every table, verified by a pre-(b) checklist), DR-5 (export + deletion within 60 s), DR-8 (axe), DR-11 (encrypt at rest via Supabase tier, posture documented in `docs/security.md`).

## Architecture references

- Patterns to respect:
  - **Authorization** (architecture.md §Authentication & Security, lines 136-141): `apps/api` connects with the Supabase **service role (bypasses RLS)**; primary enforcement is `requireUserContext()` + `where: { userId }` on every Prisma query + the `no-prisma-query-without-user-id` oxlint rule. ⚠️ The doc's clause at line 139 — "if a repository accidentally omits the `userId` guard, RLS catches the bug" — is **inaccurate on the service-role path** (RLS is bypassed there); story 11-3 corrects this wording in architecture.md AND ADR-0013.
  - **RLS coverage automation** (architecture.md:142, :239, :638): SQL probe over `pg_tables` asserting the policy quartet (domain tables) / INSERT+SELECT only (audit tables); build fails on drift. Script lives at `apps/api/scripts/rls-audit.ts` (`bun run db:rls-audit`) and already exists — 11-3 wires it as a CI gate in `.github/workflows/pr.yml`. Satisfies NFR-8 + DR-4.
  - **Repository test pattern** (architecture.md:619): every `*.repository.ts` ships ≥1 happy-path + 1 RLS-isolation test (call from another `userId` returns empty/404).
  - **At-rest encryption posture**: Supabase tier default-encrypted (DR-11 / NFR-14); offline PWA cache = encrypted IndexedDB scoped per `user_id`, key derived via Web Crypto `SubtleCrypto.deriveKey` (ADR-0003); Bridge token columns are Vault-backed but **vestigial NULL** — Bridge v3 keeps tokens server-side, nothing persisted (ADR-0015 post-impl pivot).
- ADRs in scope: `adr/0013-prisma-rls-defense-in-depth.md` (to be corrected), `adr/0015-bank-aggregator-bridge-with-provider-abstraction.md` (encryption posture / Vault pivot), `adr/0003-pwa-offline-cache-encrypted-indexeddb.md` (client at-rest), `adr/0014-prisma-migrations.md` (RLS DDL appended manually), `adr/0001-audit-history-via-sister-tables.md` (append-only RLS).

## UX references

- Screens: n/a — Epic 11's UI lives in 11-1 / 11-2 / 11-5 (settings). Story 11-3 is backend / CI / docs only, no screen.
- Components: n/a for 11-3.

## Project context (brownfield only)

- Existing patterns the epic must respect:
  - Every user-scoped table: `user_id UUID NOT NULL REFERENCES auth.users(id) ON DELETE CASCADE` + four `auth.uid() = user_id` policies (audit tables: INSERT + SELECT only).
  - RLS policy DDL is appended manually to Prisma migration SQL — Prisma does not introspect Postgres policies (ADR-0014).
  - Single-user-by-RLS, multi-user-ready; `apps/api` uses the service-role connection, `apps/web` the anon key (Supabase Auth only).
  - Tests now exist: vitest (`apps/web`) + `bun test` (`apps/api`). The project-context "no tests" note is stale post-Epic 0.
  - `bun --filter=@pekulo/api` (workspace name, not folder name).

## Lessons applicable

- **2026-05-27 (Scope: aped-review, aped-dev)** — never trust a single-tenant / single-item sandbox to prove isolation; issue the call with a BOGUS / second value and assert empty/different. → the cross-tenant isolation test in 11-3 MUST use TWO users and assert user A cannot read/mutate user B's rows (not merely that a single user's query returns rows).
- **2026-05-19 (Scope: aped-story, aped-dev, aped-review)** — `bun --filter=<name>` needs the workspace package name; use `bun --filter=@pekulo/api …` in every T-task command.
- **architecture.md:619 pattern** — repository RLS-isolation test (cross-`userId` → empty/404) is the established shape to extend for the cron/webhook path.

## Previous stories — outcomes

### Story 11-3-rls-audit-and-encryption-doc — done 2026-05-28T23:26:50Z

- **Decisions:** RLS coverage CI gate = **static migration-SQL parse** (`rls-migration-audit`, no DB) — the DB-backed `rls-audit` is kept for local/post-deploy exact policy counts, NOT wired into CI (the secret was never provisioned). Tenant isolation on the `apps/api` path is **single-layer** (the `no-prisma-query-without-user-id` lint rule + `where:{userId}`); RLS only guards the `apps/web` anon path — the service role bypasses RLS and no table sets `FORCE ROW LEVEL SECURITY`.
- **Files:** `apps/api/scripts/rls-migration-audit.ts` (+`.test.ts`), `apps/api/package.json` (`db:rls-migration-audit`), `.github/workflows/pr.yml` (`rls-audit` job), `bank-aggregator.integration.test.ts` (cross-tenant), `docs/security.md`, `docs/adr/0013-…`, `docs/architecture.md` (RLS wording + CI-job description).
- **Contracts:** none new (no API/schema change). Exports the pure `auditMigrationSql(sql, opts)` from the gate script; adds the `db:rls-migration-audit` script + the `rls-audit` CI job.
- **Deviations from plan:** static migration-text gate instead of the removed DB-backed CI job (locked at design); exported pure `auditMigrationSql` rather than the story's duplicated-in-test parser; **AC-4 `architecture.md` correction landed in review via aped-course** (the dev-time upstream-lock hook blocked it mid-dev); during review the gate regex was hardened (unquoted names + empty-dir fail-closed) and the AC-5 test gained a shared-providerItemId case that makes the per-owner `where:{userId}` scoping load-bearing.

# Epic 1 — Context Cache

_Generated by aped-story at 2026-05-09T00:00:00Z. Consumers: aped-dev, aped-review. Do not hand-edit — re-run aped-story to refresh._

## Scope from PRD

- FRs in this epic:
  - FR-1 — Declare a compass (target capital in EUR + horizon year between current+1 and current+60).
  - FR-2 — Edit the compass at any time, prior values archived in an audit trail viewable from settings.
  - FR-3 — Add up to 20 milestones per compass (target capital + target year strictly < compass horizon).
  - FR-4 — Reorder, edit, delete milestones; list re-sorted by year ascending.
  - FR-5 — Compute compass progress = current total wealth ÷ target capital, expressed as a percentage with 1 decimal.
  - FR-6 — Per-milestone status one of {ahead, on-track, behind} based on a linear plan.
  - FR-7 — Compass-progress curve (projected vs actual wealth) on the dashboard.
  - FR-8 — Flag compass setup incomplete when no milestone exists; surface setup CTA instead of progress.

- NFRs that bind:
  - NFR-1 — Compass-progress computation p95 < 300 ms on `/dashboard` (Persona Alex's volume: ≤ 200 tx/mo, ≤ 30 holdings, ≤ 5 properties).
  - NFR-3 — Lighthouse Performance ≥ 90 on `/dashboard`.
  - NFR-4 — TTFMP < 2.5 s on mid-Android 4G on `/dashboard`.
  - NFR-8 — 100 % per-row RLS coverage via `auth.uid() = user_id` on every user-scoped table; build-failing CI check.
  - NFR-22 / NFR-23 / NFR-24 — WCAG 2.2 AA, 4.5:1 / 3:1 contrast, full keyboard nav. Story 1-4 surfaces.
  - NFR-28 — Web tier opens 0 direct DB connections; `apps/api` is sole Postgres consumer.

- DRs that bind:
  - DR-4 — RLS quartet on every user-scoped table; audit tables INSERT+SELECT only (no UPDATE/DELETE policy).

## Architecture references

- **Patterns to respect:**
  - **Module factory shape (ADR-0009)** — `apps/api/src/modules/<name>/{<name>.module.ts, <name>.routes.ts, <name>.service.ts, <name>.repository.ts, <name>.errors.ts, <name>.schema.ts, <name>.types.ts}` plus `<name>.{module,handler,service,repository}.test.ts`. Factories return `{ router, service }`, `router: AnyElysia` or oRPC implement chain. **L8 (Elysia 1.4 invariant) explicitly cites story 1-1 — never annotate `Elysia`, infer or use `AnyElysia`.**
  - **Hard layering (ADR-0010, lint-enforced)**: Component → Custom Hook → Server Action (`<feature>-actions.ts`) → oRPC client → Elysia handler → service → repository → Prisma. Lint rules `no-server-action-in-component`, `no-cross-feature-action-import`, `no-prisma-query-without-user-id` all gate this path (story 0-12 shipped them).
  - **Audit history pattern (ADR-0001) — sister tables.** `compass_history` is an append-only sibling of `hypotheses`. Indexed on `(user_id, valued_on desc)`. RLS = INSERT + SELECT policies only (no UPDATE / no DELETE) — append-only enforced at the policy layer. Compass updates write to `hypotheses` + insert into `compass_history` within the **same Prisma `$transaction`** (architecture L593).
  - **RLS defense in depth (ADR-0013)** — every Prisma query carries explicit `where: { userId: ctx.userId, … }`; RLS stays active even if `apps/api` omits the guard. Story 0-12's `pekulo/no-prisma-query-without-user-id` rule blocks omissions in `apps/api/src/modules/**/*.repository.ts`.
  - **Prisma error mapping** — repository wraps Prisma errors via `prisma-error-mapper.ts` → `RlsViolationError` on `P2025` if the row didn't match the userId guard.
  - **Decimal coercion (L24, 2026-05-04)** — Prisma `Decimal` columns must be coerced via `.toNumber()`, not `Number(decimal)`. Apply at the repository → service → DTO boundary (existing pattern in `hypothesis.service.ts#decimalToNumber`). Story 1-1 explicitly named in L24's scope list.
  - **Prefixed IDs (ADR-0012)** — `CompassHistory.id` = `cph_{base62-21}` injected by the prefixed-ids extension. Already registered in `apps/api/src/database/id-prefixes.config.ts`. No change needed.
  - **Migration discipline (ADR-0014)** — `prisma migrate dev --name <verb_noun>` writes `apps/api/prisma/migrations/<timestamp>_create_compass_history/`. Forward-only. **The 4 RLS policies (audit variant: INSERT + SELECT only) are appended manually to the migration SQL — Prisma does not introspect policies.** `rls-audit` CI re-asserts coverage.
  - **Naming (Phase 3)** — Prisma model `CompassHistory` PascalCase; table `compass_history` plural snake_case (new); contract module key `compass`, mount path `/rpc/v1/compass`.

- **ADRs in scope:**
  - `docs/adr/0001-audit-history-via-sister-tables.md` — sister-table audit pattern (load-bearing for `compass_history`).
  - `docs/adr/0009-elysia-orpc-with-zapaction-bridge.md` — module factory + oRPC mount.
  - `docs/adr/0010-hooks-orchestration-boundary.md` — hard layering (forward-pointer for 1-4).
  - `docs/adr/0012-prisma-7-schema-folder-prefixed-ids.md` — `cph` prefix already registered.
  - `docs/adr/0013-prisma-rls-defense-in-depth.md` — explicit `where: { userId }` clause.
  - `docs/adr/0014-prisma-migrations.md` — Prisma migrate + manual RLS append.

## UX references

- **Screens (from `docs/ux/screen-inventory.md`):**
  - Screen 1 — Cap view (`/dashboard?tab=cap`) — primary surface for compass + milestones (FR-1 → FR-8 per inventory). Story 1-4 ships the UI; stories 1-1 → 1-3 ship domain only.
  - Screen 7 — Settings (`/parametres`) — compass edit form + history panel surface (story 1-4).
- **Components (from `docs/ux/components.md`):**
  - `DonutCard` + `Donut` (large variant) — FR-5, FR-7 (UI only, story 1-4).
  - `MilestonesSection` / `MilestonesCard` + `MilestoneRow` + `StaggerList` — FR-3, FR-4, FR-6 (UI only, story 1-4).
  - `compass-edit-form`, `add-milestone-form`, `compass-history-panel`, `compass-setup-cta` — story 1-4.

> Stories 1-1, 1-2, 1-3 are **API-only**. UI surfaces ship in 1-4 (which depends on 1-1+1-2+1-3+0-10).

## Project context (brownfield only)

- **Compass schema is brownfield-aware (project-context L42, L205)**: `hypotheses.objectif` (target capital) + `hypotheses.horizon_years` already encode the "compass" decided in the grill. **No migration needed for the carrier columns** — Story 1-1 only adds the new `compass_history` audit sister table. The `Hypothesis` Prisma model lives in `apps/api/prisma/schema/hypothesis.prisma` with `@@map("hypotheses")`.
- **Existing Hypothesis service (`apps/api/src/modules/hypothesis/hypothesis.service.ts`)** owns the get/save of the brownfield row including `objectif` + `horizonYears`. Story 1-1 introduces a **new `compass` module** that operates on the same `Hypothesis` row (read `objectif` + `horizonYears`, write the audit trail) but exposes the **compass-shaped contract** (per `compass.contract.ts` — currently empty scaffold from story 0-5). Do **not** re-implement the hypothesis CRUD inside the compass module; the two modules co-exist.
- **Existing patterns to mirror:**
  - `decimalToNumber()` helper in `hypothesis.service.ts:28-39` (apply at the row → DTO boundary).
  - `createXxxModule(deps): { router, service }` factory pattern (`hypothesis.module.ts:14`).
  - `implement(contract).$context<{ userId, email }>().router({ … })` oRPC handler shape (`hypothesis.routes.ts:15-35`) — context injection via mountOrpc, handlers throw `PekuloError("UNAUTHORIZED", …)` when context missing.
  - `prisma.upsert({ where: { userId }, … })` defense-in-depth (`hypothesis.service.ts:157-163`).
- **Web tier still uses zapaction (story 0-6 bridge)**: any web-side hook / action work in 1-4 will go through `'use server'` thin wrappers over the oRPC client.

## Lessons applicable

- **L8 — 2026-05-04 — Elysia 1.4 `Elysia` type is invariant** (Scope: aped-arch, aped-story, aped-dev — explicitly lists **story 1-1**)
  - Rule: never annotate variables/parameters as bare `Elysia`. Module factories let TS infer chains; cross-cutting boundaries import `AnyElysia` from `elysia`. The compass module's router type should follow the same shape as `hypothesis.module.ts` — return type inferred from `createCompassRouter`.

- **L24 — 2026-05-04 — `Number(decimal)` silently truncates above MAX_SAFE_INTEGER** (Scope: aped-arch, aped-dev — explicitly lists **story 1-1**)
  - Rule: coerce Prisma `Decimal` via `.toNumber()` at the boundary. Apply to `objectif` (column type `Decimal`) when reading from `Hypothesis`. Existing `decimalToNumber()` helper in `hypothesis.service.ts` is the pattern — duplicate or extract to `apps/api/src/common/derive/decimal.ts` if reused (out-of-scope of 1-1 unless trivial).

- **L25 — 2026-05-04 — `AsyncLocalStorage.enterWith` correctness rests on Next.js per-request `ResourceContext` isolation** (Scope: aped-arch, aped-dev — stories 0-6, 0-7, every apps/web feature port)
  - Rule: re-verify on every Next minor bump. Story 1-1 is API-only; rule applies forward to story 1-4's web hook work, not to this story.

- **L23 — 2026-05-04 — Bun `--frozen-lockfile` in Docker requires every workspace member's `package.json`** (Scope: aped-arch, aped-story, aped-dev — every `apps/*` Dockerfile)
  - Rule: when adding new packages, ensure `.dockerignore` re-includes manifests via `!path` exceptions. Story 1-1 does not add a new workspace; rule informs architecture only.

- **Story 0-12 lint rules now active** (general — applies to every API story from epic 1 onward):
  - `pekulo/no-prisma-query-without-user-id` — every `prisma.compassHistory.*` and `prisma.hypothesis.*` query in `compass.repository.ts` MUST include `where: { userId }` (or `findUnique({ where: { userId } })` for single-row models). The rule's `prismaIdentifier` accepts `string | string[]` — repositories using `prisma.$transaction(async (tx) => …)` should pass `["prisma","tx"]` opt-in.
  - `pekulo/no-server-action-in-component` — irrelevant to story 1-1 (API-only).
  - `pekulo/no-cross-feature-action-import` — `compass-actions.ts` (when 1-4 lands) MUST NOT import from `hypothesis-actions.ts`. Cross-feature work goes via global hooks layer.
  - `pekulo/no-tailwind-outside-ui` — irrelevant to story 1-1.

## Previous stories — outcomes

### Story 1-1-compass-domain — done 2026-05-09T15:57:00Z

- **Decisions:**
  - `decimalToNumber` extracted to `apps/api/src/common/derive/decimal-to-number.ts` — single mechanism for L24 across 1-2/2-1/3-1/4-1/5-1; downstream stories MUST consume it (do NOT inline `Number(decimal)` or duplicate the helper).
  - `PekuloErrorCode` is no longer closed at the union level — modules add their own codes by extending the union + `PEKULO_ERROR_CODES` set in `apps/api/src/common/errors/pekulo-error.ts` AND adding the HTTP status in `apps/api/src/platform/http/error-mapper.ts#ORPC_HTTP_STATUS_BY_CODE`. `PekuloError.name` typing is `string` (not literal) so subclasses can override; `isPekuloError` still demands runtime exact match.
  - Lint rule `pekulo/no-prisma-query-without-user-id` runs in `apps/api/**` with `prismaIdentifier: ["prisma","tx"]` — repositories using `$transaction(async tx => …)` are covered. Future story repos under `apps/api/src/modules/**/*.repository.ts` inherit this override automatically.
  - Live-DB test harness deferred — `apps/api/src/test/helpers/test-db.ts` was scoped out; AC-5 / AC-6 lean on lint + `apps/api/scripts/rls-audit.ts`. First downstream story needing live-DB integration tests builds the harness then.
  - Hypothesis service refactor to consume the extracted `decimalToNumber` is OUT OF SCOPE — track on a later story (1-2 plausibly).

- **Files:**
  - Created: `apps/api/prisma/schema/compass.prisma`, `apps/api/prisma/migrations/20260509150000_create_compass_history/migration.sql`, `apps/api/src/common/derive/{decimal-to-number,compass-progress}{,.test}.ts`, `apps/api/src/modules/compass/{errors,types,repository,service,routes,module}.ts` + 4 test files (`{repository,service,module,integration}.test.ts`), `packages/validators/src/compass.ts`.
  - Modified: `packages/validators/src/index.ts`, `packages/contracts/src/compass.contract.ts`, `apps/api/src/bootstrap/runtime-dependencies.ts`, `apps/api/src/common/errors/pekulo-error.ts`, `apps/api/src/platform/http/error-mapper.ts`, `apps/api/scripts/rls-audit.ts`, `apps/api/package.json`, `.oxlintrc.json`, `.gitleaks.toml`.

- **Contracts:**
  - `@pekulo/validators#updateCompassInputSchema` (objectif > 0 ∧ ≤ 1e12, horizonYears ∈ [1, 60]), `compassSchema`, `compassSetupStateSchema`, `compassSetupStateOutputSchema` — schema-side outputs that 1-2/1-3/1-4 consume directly (do NOT redefine compass shapes elsewhere).
  - `@pekulo/contracts#compassContract` — 3 oRPC procedures (`updateCompass`, `getCompass`, `getSetupState`) under `/rpc/v1/compass`.
  - Domain types `Compass`, `CompassSetupState`, `MilestonePresenceProbe`, `CompassHistoryEntry` exported from `apps/api/src/modules/compass/compass.types.ts` — story 1-2 swaps the stub `MilestonePresenceProbe` for a Prisma-backed probe wired via `runtime-dependencies.ts`.
  - 4 new `PekuloErrorCode`s reserved (`INVALID_TARGET`, `INVALID_WEALTH`, `COMPASS_NOT_FOUND`, `TRANSACTION_FAILED`) with HTTP statuses 400/400/404/500.

- **Deviations from plan:**
  - T1 migration SQL written manually (Supabase pooler hang on `prisma migrate dev`) — functionally identical, applied via `prisma migrate deploy` at deploy time.
  - T11 fake-Prisma instead of live-DB harness (test-db helper deferred); AC-5/AC-6 covered by lint + `db:rls-audit` per pre-implementation user decision.
  - T10 module mounted via `runtime-dependencies.ts` (not `app.ts` as spec said) — `runtime-dependencies.ts` is the single source for module instantiation; `app.ts` calls `mountOrpc(app, { orpcRouter })` with the assembled router.
  - aped-review hardenings (commit `2ba94eb`): NaN/Infinity guards on `computeProgress`, fake-Prisma now models `$transaction` rollback, JWT branches (expired/wrong-issuer/wrong-audience) added to integration test, `orderBy` tie-break on `createdAt`, `listHistory` limit clamped `[1, 200]`, `objectif` capped at 1e12, route guards reject whitespace-only userId, probe-skip + probe-rejection tests added.

### Story 1-2-milestones-domain — done 2026-05-09T18:13:00Z

- **Decisions:**
  - Milestones domain constants (`MILESTONES_PER_USER_CAP`, `MAX_TARGET_CAPITAL_EUR`, `MAX_LABEL_LENGTH`, `MILESTONE_STATUS_TOLERANCE_RATIO`) and compass bounds (`MAX_OBJECTIF_EUR`, `MIN_HORIZON_YEARS`, `MAX_HORIZON_YEARS`) live as SSOT in `@pekulo/validators` and are re-exported from `@pekulo/types` per-domain section. Downstream stories MUST import from `@pekulo/types` — no inlined literal `20` / `1_000_000_000` / `0.05`. Extends the 2026-05-09 types-centralization invariant to runtime constants.
  - Compass `horizonYears.min` bumped 1 → 2 (review F2): the milestones service's `[currentYear+1, currentYear+horizonYears-1]` range is empty at horizonYears=1. Stories 1-3 / 1-4 inherit the new lower bound — UI forms must enforce min=2.
  - Cap enforcement on milestone insert is now atomic: `repository.addEnforcingCap(userId, input, cap)` wraps count+create in `prismaService.client.$transaction` and returns `Milestone | { capExceeded: true }`. Future repos that need check-then-act semantics on an unbounded user-scoped table should follow the same pattern (sentinel return, not exception out of transaction).
  - `MAX_TARGET_CAPITAL_EUR` is 1e9 (not 1e12 like compass `MAX_OBJECTIF_EUR`) — write path goes through plain `number` and Decimal coercion happens server-side; tighter bound keeps targetCapital inside JS Number safe-integer range.
  - PekuloError thrown inside an oRPC handler is wrapped by RPCHandler before the Elysia error-mapper sees it. Integration tests assert wire-level happy paths only; cross-user 404 and similar typed-error mappings live at the service-test layer.

- **Files:**
  - Created: `apps/api/prisma/schema/milestones.prisma`, `apps/api/prisma/migrations/20260510120000_create_milestones/migration.sql`, `packages/validators/src/milestones.ts`, `apps/api/src/common/derive/milestone-status.ts` + `.test.ts`, `apps/api/src/modules/milestones/{errors,repository,service,routes,module}.ts` + 4 test files (`{repository,service,module,integration}.test.ts`).
  - Modified: `apps/api/prisma/schema/enums.prisma` (MilestoneStatus enum), `packages/validators/src/{index,compass}.ts`, `packages/contracts/src/milestones.contract.ts`, `apps/api/src/bootstrap/runtime-dependencies.ts`, `apps/api/src/common/errors/pekulo-error.ts`, `apps/api/src/platform/http/error-mapper.ts`, `apps/api/scripts/rls-audit.ts`, `apps/api/package.json`, `packages/types/{package.json,src/index.ts}`, `packages/validators/package.json`, `packages/ui/src/components/PekuloMilestone{,s}*.tsx`. Story 1-1's `compass.types.ts` deleted (types-centralization refactor) — `compass.{module,repository,service,service.test}.ts` re-routed to `@pekulo/types`.

- **Contracts:**
  - `@pekulo/validators#{addMilestoneInputSchema, updateMilestoneInputSchema, deleteMilestoneInputSchema, milestoneSchema, milestoneStatusSchema, milestoneStatusEntrySchema, getStatusesInputSchema, getStatusesOutputSchema, listMilestonesOutputSchema}` — 9 schemas. Stories 1-3 / 1-4 consume `milestoneSchema` and `milestoneStatusEntrySchema` directly via `@pekulo/types`.
  - `@pekulo/contracts#milestonesContract` — 5 oRPC procedures (`add`, `update`, `delete`, `list`, `getStatuses`) under `/rpc/v1/milestones`.
  - Domain types `Milestone`, `MilestoneStatusEntry`, `CompassReader`, `MilestonePresenceProbe`, `Compass`, `CompassSetupState`, `CompassHistoryEntry` exported from `@pekulo/types`. The legacy V1 design-system row shape `Milestone` was renamed `MilestoneCardItem` to free the canonical name.
  - 5 new `PekuloErrorCode`s reserved (`MILESTONE_LIMIT_EXCEEDED`, `MILESTONE_YEAR_OUT_OF_RANGE`, `MILESTONE_NOT_FOUND`, `MILESTONE_INVALID_CAPITAL`, `COMPASS_REQUIRED`) with HTTP statuses 409/400/404/400/409.
  - Compass module's `MilestonePresenceProbe` (story 1-1 stub) is now wired to `milestonesModule.presenceProbe` in `runtime-dependencies.ts` — `compass.getSetupState` returns `'complete'` when ≥ 1 milestone exists.

- **Deviations from plan:**
  - T1 migration SQL written manually (Supabase pooler hang precedent from story 1-1); functionally identical, applied via `prisma migrate deploy` at deploy time.
  - Mid-story refactor (commit `87f137d`): `compass.types.ts` and `milestones.types.ts` deleted and lifted to `@pekulo/types`. Reversed `@pekulo/types ↔ @pekulo/validators` dep direction so types depends on validators (validators stays runtime SSOT).
  - aped-review hardenings (commits `b82582f` / `3039e7e` / `a697e18`): TOCTOU on cap fixed via `$transaction`-wrapped `addEnforcingCap`; `horizonYears.min` 1 → 2; `MAX_TARGET_CAPITAL_EUR` 1e12 → 1e9; `position: 0` written explicitly (drop wide cast); domain constants centralized in `@pekulo/types` (incl. `MILESTONE_STATUS_TOLERANCE_RATIO` lifted from `milestone-status.ts`); `setTimeout(30ms)` server-bind replaced with awaited `app.listen` callback; warm-up fetch added before AC-13 latency check; integration coverage extended to update/delete/getStatuses; AC-9 fixture asserts full numeric vectors + determinism re-run; AC-11 cross-module probe substitution proved end-to-end via `compass.module.test.ts`. Final test pass: 142 / 0 (was 135 / 0 pre-review).

### Story 1-3-compass-curve — done 2026-05-09T19:48:00Z

- **Decisions:**
  - `WealthHistoryProvider` adapter pattern is the canonical way to bridge the compass module to brownfield wealth data (mirrors `CompassReader` from story 1-2). The interface lives in `@pekulo/types`; the Prisma-backed closure lives in `runtime-dependencies.ts`. Epic 5's monthly module port should expose the SAME interface so the compass module never touches `MonthlyTracking` directly. Downstream consumers MUST inject the provider, never read `monthly_tracking` from inside the compass module.
  - Service `clock` dep (`() => Date`, default `() => new Date()`) is the canonical determinism mechanism for any time-aware service method going forward — preferred over an explicit `today` param on the public method (keeps the wire contract clean). Tests inject `() => new Date('<ISO>')`. Apply to every future service that reads the wall clock.
  - `decimalToNumber(row.capitalTotal, 0)` from `apps/api/src/common/derive/decimal-to-number.ts` (story 1-1 extract) is the ONLY mechanism for L24 in epic 1+. The wealth provider closure consumes it; do NOT inline `Number(decimal)` and do NOT duplicate the helper. AC-9 is layer-pinned at the service (large finite totalEur surfaces unchanged); the actual Decimal coercion test lives in story 1-1.
  - `28-of-month UTC` is the safe last-of-month proxy used by the wealth-provider adapter (`new Date(Date.UTC(year, monthNum - 1, 28))`) — valid in every month, no leap-year concerns. Reuse this anchor for any future brownfield monthly-aggregate adapter.
  - aped-review F5 in-scope override: a new migration `20260510130000_index_monthly_tracking_curve` was added (despite the story's "no new migration" rule) to align the `monthly_tracking` index column order with the wealth-provider `orderBy`. Future stories that hit a brownfield query-pattern mismatch can override the no-migration rule with explicit user confirmation; record the override in the Review Record.

- **Files:**
  - Created: `apps/api/src/common/derive/compass-curve.ts` + `.test.ts`, `apps/api/prisma/migrations/20260510130000_index_monthly_tracking_curve/migration.sql`.
  - Modified: `apps/api/src/modules/compass/{compass.repository,compass.service,compass.routes,compass.module}.ts` + their `.test.ts` siblings + `compass.integration.test.ts`; `apps/api/src/bootstrap/runtime-dependencies.ts`; `packages/validators/src/compass.ts`; `packages/types/src/index.ts`; `packages/contracts/src/compass.contract.ts`; `apps/api/prisma/schema/monthly.prisma`.

- **Contracts:**
  - `@pekulo/validators#{compassCurvePointSchema, compassCurveSchema}` + Zod-inferred `CompassCurve` / `CompassCurvePoint` types. Story 1-4 (Cap UI) consumes the curve schema directly via `@pekulo/types` re-export.
  - `@pekulo/types#{WealthSnapshot, WealthHistoryProvider}` — the adapter interface for any monthly wealth source. Epic 5 will swap the runtime closure for a proper module wiring without changing the interface.
  - `@pekulo/contracts#compassContract` extended with `getCompassCurve: oc.output(compassCurveSchema)` (no input). Mounted at `POST /rpc/v1/compass/getCompassCurve`. Story 1-4 calls this from the dashboard.
  - `CompassService` interface gains `getCompassCurve(userId): Promise<CompassCurve>`. Factory deps gain `wealthHistoryProvider: WealthHistoryProvider` AND optional `clock: () => Date`.
  - No new `PekuloErrorCode` (reuses `COMPASS_NOT_FOUND` 404, `TRANSACTION_FAILED` 500, `INVALID_TARGET` 400, `INVALID_WEALTH` 400 — all from story 1-1).

- **Deviations from plan:**
  - Bundled into 4 feat commits (T1+T2+T4 / T5 / T6+T8+T9 / T3+T7+T10) due to typecheck-coupling between layered changes — not the planned one-commit-per-task. Documented in Dev Agent Record.
  - aped-review F5 override: new migration added (story spec said "no new migration"). User explicitly accepted the override.
  - aped-review hardenings (commit `fcebf70`): +4 boundary tests on `computeCompassCurve` (today===startDate, today===endDate, today>endDate, snapshot==endDate dedup); migration `20260510130000_index_monthly_tracking_curve` for query-pattern alignment; File List doc fix (`packages/validators/src/index.ts` is wildcard-covered, not touched). Final test pass: 172 / 0 / 429 expect() (was 168 / 0 / 421 pre-review).

### Story 1-4-compass-ui-cap — done 2026-05-15T14:11:00Z

- **Decisions:**
  - Cap-view layout (shell + bento + cards) is driven by ONE CSS module behind `@media (min-width: 1020px)` — Tamagui responsive props (`$lg`, `$md`) MUST NOT touch grid-cell sizing or sidebar clearance because Pekulo Tamagui `lg = 1280` ≠ Tailwind `lg = 1024` (mismatch creates a sidebar-overlap window). Future apps/web stories that port a `docs/ux-preview/` mockup follow the same single-source rule. Lesson `2026-05-13` documents the mechanism.
  - Bento `grid-auto-rows: minmax(220px, auto)` — the 112 px floor lifted from ux-preview was a mobile-mock relic that did not pair with a 208 px donut. Every bento cell is `display: flex; flex-direction: column` with `> * { flex: 1; min-height: 0 }` so inner Section consumes the stretched cell height. Growing lists (milestones today, transactions / holdings tomorrow) MUST use `flex: 1; minHeight: 0; overflowY: auto` so the row track stays invariant. Apply to story 7-1's bento composition, every future dashboard tab.
  - Write-path forms inside a bento cell go through `PekuloDialog` (mounted at the page level via a context provider for shared open-state), NOT inline-reveal. Inline-reveal blew the bento row track when the form expanded the cell. The shared `<AddMilestoneDialogProvider>` pattern (page-level mount, single Portal, `useAddMilestoneDialog()` hook consumed by both the header pill and the empty-state CTA) is the canonical way to share a write-path modal across siblings in the same surface.
  - `PekuloMilestoneRow.onDelete?` extension: optional callback + `isDeleting` flag at the DS level (route-local concern OK because the row is the only place a delete affordance fits visually). `MilestoneCardItem.id?` plumb through the derive helper. Pattern generalises — any future row-shape that needs per-row affordances exposes the optional callback at the DS level.
  - Donut math (`progressPct` in `deriveMilestoneCardItems`) uses **linear-plan ratio** `linearPlanForYear / targetCapital` — NOT `currentWealth / targetCapital`. The latter renders a near-empty donut for far-future paliers even when on plan; the former mirrors ux-preview's MilestoneRow logic and reads as "how much of this palier am I projected to hit at its target year". Compass.objectif + horizonYears feed `annualPlan` and are required by the helper (graceful fallback to snapshot ratio when missing).
  - Multi-pass review-record discipline: Pass-N "APPROVED post-fix" claims MUST be verified against `git show HEAD:` BEFORE writing. Pass 1 (2026-05-09) committed an "APPROVED" record citing fixes that lived only in the working tree; Pass 2 (2026-05-13) caught it via `git ls-tree HEAD` and rewrote the record with commit-hash citations. Lesson `2026-05-13` codifies the verification workflow.

- **Files:**
  - Created: `apps/web/src/lib/{actions/{compass,milestones}-actions,data/{compass,milestones},derive-milestone-card-items{,.test}}.ts`, `apps/web/src/app/(cap)/dashboard/{layout.tsx,page.tsx,_components/{cap-shell,compass-section,compass-setup-cta,add-milestone-form{,.spy.test,.a11y.test},add-milestone-dialog,milestones-section{,.a11y.test},compass-section.a11y.test,bento.module.css,placeholder-card,form-primitives}.tsx,_hooks/{use-dashboard-compass,use-compass-curve,use-milestones,use-milestone-statuses,use-add-milestone-form{,.test},use-update-milestone{,.test},use-delete-milestone{,.test}}.ts,parametres/{page.tsx,_components/{compass-edit-form{,.a11y.test},compass-history-panel{,.a11y.test}},_hooks/{use-update-compass{,.test},use-edit-compass-form,use-compass-history}}.ts*}` ; `apps/web/src/app/(cap)/_components/form-primitives.tsx` ; `apps/web/{vitest.config.ts,test/{setup.tsx,zapaction-core-stub.ts,server-only-stub.ts,next-script-stub.tsx,vitest-axe.d.ts}}`.
  - Modified: `packages/{validators,contracts}/src/compass.{ts,contract.ts}` ; `packages/types/src/index.ts` (`MilestoneCardItem.id?`) ; `packages/ui/src/components/{PekuloDonut,PekuloMilestoneRow,PekuloNavRail}.tsx` ; `packages/oxlint-config/src/rules/no-server-action-in-component.js` (exempt `_hooks/_actions/` paths) ; `apps/api/src/modules/compass/{compass.service{,.test},compass.routes,compass.module.test,compass.integration.test}.ts` ; `apps/api/{prisma/schema/hypothesis.prisma,src/{bootstrap/runtime-dependencies,database/{id-prefixes.config{,.test},prefixed-ids.injector{,.test}},platform/security/jwt-verifier}.ts}` (brownfield: Hypothesis native UUID + ES256 JWT support) ; `apps/web/src/app/{layout.tsx,(cap)/dashboard/layout.tsx}` ; `apps/web/src/lib/{orpc/{client,modules},zapaction/keys}.ts` ; `apps/web/package.json` (react-grab dev dep).

- **Contracts:**
  - `@pekulo/validators#{compassProgressSchema, compassHistoryEntrySchema, listHistoryInputSchema}` + Zod-inferred `CompassProgress`. Story 7-1 (Cap dashboard composition) consumes both via `@pekulo/types` re-export.
  - `@pekulo/contracts#compassContract` extended with `getCurrentProgress: oc.output(compassProgressSchema)` and `listHistory: oc.input(listHistoryInputSchema).output(z.array(compassHistoryEntrySchema))`. Mounted at `POST /rpc/v1/compass/{getCurrentProgress,listHistory}`.
  - `CompassService` gains `getCurrentProgress(userId): Promise<CompassProgress>` and `listHistory(userId, opts?): Promise<CompassHistoryEntry[]>`.
  - `MilestoneCardItem.id?: string` (optional) — derive helper plumbs domain id so DS rows (`PekuloMilestoneRow`) can route per-row affordances back to mutation hooks. Legacy mockups without an id keep typechecking.
  - `PekuloDonut` gains `ariaLabel?: string` (svg becomes `role="img"` when provided). `PekuloMilestoneRow` gains `onDelete?: (id: string) => void` and `isDeleting?: boolean` (renders `Trash2` pill on the right with `aria-label="Supprimer le palier <label>"`).
  - `Hypothesis` model registered with `null` in `id-prefixes.config.ts` — brownfield UUID column opt-out; future brownfield models with native `@db.Uuid` columns follow the same pattern.
  - `JwtVerifierInput.supabaseUrl?` — when provided, `jwt-verifier.ts` accepts ES256 tokens via `createRemoteJWKSet`. HS256 path remains live (Docker-local + integration tests).

- **Deviations from plan:**
  - Multi-pass fix-cycle: 8 review passes (Pass 1 superseded by Pass 2 — see decisions). Final commit count on the branch: 31 ahead of `main`. Story spec assumed a single dev → review cycle; the 8 passes reflect a real browser-driven visual refinement loop that the auditor matrix cannot catch (auditors don't render at viewport sizes between 1024 and 1280 px).
  - 5 placeholder cards (`<PlaceholderCard variant="…">`) shipped for the Cap-view bento cells owned by future stories (HeroCard / TrajectoryCard / 7-1, CompositionCard / RecentActivityCard / 5-x, HypothesisCard / 6-x). Each placeholder renders a structural skeleton + "Bientôt · {ownerStory}" footnote. Story 7-1 / 5-x / 6-x swap the placeholder import for the real DS card.
  - Brownfield infra additions surfaced while wiring story 1-4 against a real Supabase project (Hypothesis UUID + ES256 JWT) were originally scoped out — landed in commit `3831d22` because they were the difference between "compiles" and "actually works against prod auth".
  - `+€ vs plan` perf-delta line on the donut card collapses (no data) until story 7-1 wires `computeAhead()` against the live `useCompassCurve` data. The donut card structurally has the line; only the numeric is deferred.
  - Patrimoine top-tab toggle is non-functional (toasts "Bientôt") — the second view is owned by a separate Patrimoine story.
  - aped-review patches landed across passes 5-8: `(cap)/parametres/ → (cap)/dashboard/parametres/` route relocation (Pass 5 CRIT-1), useDeleteMilestone wired into MilestonesSection (Pass 5 CRIT-2), AC-9 mutation hook tests committed (Pass 5 HIGH-4), AC-10 listHistory happy + getCurrentProgress 401 integration tests (Pass 5 HIGH-5), bento + shell + dialog + token sweep across passes 6-8.
